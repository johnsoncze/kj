For add a new filter type you have to:
- create required object with `IFilter` interface if is not exist already
- create block for render filter
- add way how filter will be show label if is filter filtered