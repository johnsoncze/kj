/* eslint-disable */

((($) => {
  $('.js-tooltip-hover').tooltipster({
    contentAsHTML: true,
    interactive: true
  });

  $('.js-tooltip-touch').tooltipster({
    contentAsHTML: true,
    interactive: true,
    trigger: 'click'
  });
})(window.jQuery));
